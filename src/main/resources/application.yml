spring:
  application:
    name: db-user-access-management

  # Database Configuration (PostgreSQL) - Primary app database
  datasource:
    jdbc-url: jdbc:postgresql://localhost:5432/userdb
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:password}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

  # JPA Configuration (uses PostgreSQL datasource above)
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # Thymeleaf Configuration
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML

  # Quartz Scheduler Configuration
  quartz:
    job-store-type: jdbc
    jdbc:
      initialize-schema: always
    properties:
      org:
        quartz:
          scheduler:
            instanceName: UserPermissionScheduler
          threadPool:
            threadCount: 5

# Target MariaDB Configuration - Database where users/permissions are managed
mariadb:
  datasource:
    jdbc-url: jdbc:mariadb://localhost:3306/mysql
    driver-class-name: org.mariadb.jdbc.Driver
    username: ${MARIADB_USERNAME:root}
    password: ${MARIADB_PASSWORD:rootpass}
    maximum-pool-size: 5
    minimum-idle: 2
    connection-timeout: 30000
    # Query timeout in seconds (prevents hanging queries)
    query-timeout: 30

  # MariaDB User Management Configuration
  user-management:
    # Default host pattern for created users (% = any host, localhost = local only)
    default-host: ${MARIADB_DEFAULT_HOST:%}
    # Length of auto-generated passwords (minimum 8, recommended 20+)
    password-length: ${MARIADB_PASSWORD_LENGTH:20}
    # Query timeout in seconds for user management operations
    query-timeout: ${MARIADB_QUERY_TIMEOUT:30}
    # Retry configuration for transient failures
    retry:
      # Maximum number of retry attempts for transient database errors
      max-attempts: ${MARIADB_RETRY_MAX_ATTEMPTS:3}
      # Initial backoff delay in milliseconds (exponential backoff)
      backoff-delay: ${MARIADB_RETRY_BACKOFF_DELAY:1000}
      # Backoff multiplier for exponential backoff
      backoff-multiplier: ${MARIADB_RETRY_BACKOFF_MULTIPLIER:2.0}
      # Maximum backoff delay in milliseconds
      max-backoff-delay: ${MARIADB_RETRY_MAX_BACKOFF_DELAY:5000}

# Server Configuration
server:
  port: 8080
  servlet:
    context-path: /
  error:
    include-message: always
    include-binding-errors: always

# JWT Configuration
jwt:
  secret: your-256-bit-secret-key-change-this-in-production-environment-please
  expiration: 86400000 # 24 hours in milliseconds

# API Documentation
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

# Logging Configuration
logging:
  level:
    root: INFO
    com.delta.dms.ops: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# Application Specific Configuration
app:
  permission:
    check-interval: 300000 # Check permissions every 5 minutes (in milliseconds)
    cleanup-expired: true
    grace-period: 60000 # 1 minute grace period before revoking expired permissions

  # Admin account configuration
  admin:
    username: ${ADMIN_USERNAME:admin}
    password: ${ADMIN_PASSWORD:admin123}
    email: ${ADMIN_EMAIL:admin@example.com}
    full-name: ${ADMIN_FULLNAME:System Administrator}
